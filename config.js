/* LearningMirror — CONFIG
   No key lives in this file. The site asks for an API key once in the
   browser and stores it there (localStorage), never in the repository. */
const ANTHROPIC_API_KEY = "PASTE_YOUR_API_KEY_HERE";
const MODEL = "claude-sonnet-4-6";
